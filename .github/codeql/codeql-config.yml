name: Codex Subscription Router security queries

queries:
  - uses: security-extended
  - uses: security-and-quality

paths-ignore:
  - node_modules
  - .artifacts
  - artifacts
  - dist
  - build
  - screenshots
